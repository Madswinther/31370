\ProvidesPackage{TheMadsPackEnglish}

% Set margins of the page
\usepackage{geometry}
\geometry{top = 3cm, bottom = 3cm, left = 3cm, right = 3cm}

% Character Encoding
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

% Orddeling
\usepackage[english]{babel}
\renewcommand\englishhyphenmins{22}

% Selection of fonts and symbols
\usepackage{times}					% Use "Times" as font for normal text.
\usepackage{courier}				% Use "Courier" as text
\usepackage{amsmath,amssymb}		% Use ams-math symbols in math mode.
\usepackage{mathtools}				% Improved appearance of math.
\usepackage{bm}						% Bold mathematics


% Format of captions:
\usepackage[font=small,format=plain,labelfont=bf,up,textfont=it, up]{caption}
\usepackage{subcaption}


% Format of equation counter
\numberwithin{equation}{section}


% Packages for pdf-inclusion and graphics.
\usepackage[pdftex]{graphicx}		% Improved graphics package
\usepackage{pdfpages}				% Include pdf-files in document
\usepackage{framed}					% Frame environments
\usepackage{xcolor}					% Color handling


% Misc package inclusions
\usepackage{float}					% Handle figure position
\usepackage{wrapfig}				% Wrap text around figure
\usepackage{appendix}				% Handle appendices
\usepackage{cite}					% Improved citations
\usepackage{tabularx,booktabs}		% Improved tabels
\usepackage{multirow}				% Multirow tabels
\usepackage{siunitx}				% Use SI-units in equations
\usepackage[hidelinks]{hyperref}	% Clickable hyperrefs.


% Custom commands
\newcommand{\degrees}{^\circ}
\newcommand{\code}[1]{\texttt{#1}}








